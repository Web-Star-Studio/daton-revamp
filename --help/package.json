{
  "name": "@daton/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts --port 8787 --local",
    "deploy": "node ./scripts/deploy-with-sentry.mjs",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests for @daton/api\""
  },
  "dependencies": {
    "@daton/auth": "workspace:*",
    "@daton/contracts": "workspace:*",
    "@daton/db": "workspace:*",
    "@hono/zod-validator": "^0.7.5",
    "@sentry/cloudflare": "^10.42.0",
    "drizzle-orm": "0.45.1",
    "hono": "4.12.5",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@sentry/cli": "^3.3.0",
    "@cloudflare/workers-types": "^4.20260306.0",
    "wrangler": "^4.48.1"
  }
}
